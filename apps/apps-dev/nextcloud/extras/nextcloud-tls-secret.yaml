apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: immich-tls-secret
spec:
  commonName: immich server
  dnsNames:
    - "*.wheezy.lab"
  duration: 4320h
  issuerRef:
    kind: ClusterIssuer
    name: ingress
  privateKey:
    algorithm: ECDSA
    size: 256
  secretName: immich-server-tls
  subject:
    organizationalUnits:
      - Kubernetes
    organizations:
      - wheezy.lab
