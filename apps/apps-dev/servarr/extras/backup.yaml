apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: servarr-pvc-daily
  namespace: velero
spec:
  schedule: "0 3 * * *"
  template:
    defaultVolumesToFsBackup: false
    snapshotMoveData: true
    includedNamespaces:
      - servarr
    excludedResources:
      - deployments
      - services
      - ingresses
      - configmaps
      - serviceaccounts
    ttl: 168h0m0s
